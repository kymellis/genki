{{! Detailed instructions can be found in the Documentation >> Activation >> Special sections }}


<div class="special-section">
	<div class="global-title-wrap">
		
		{{! Title }}
		<h3 class="global-title-medium">{{{t "Personal {start-italic}growth.{end-italic}" start-italic="<span>" end-italic="</span>"}}}</h3>

		{{! Link }}
		<a href="{{@site.url}}/growth/" aria-label="Link to the collection">{{>icons/site/arrow_right}}</a>

	</div>
	<div class="special-wrap">

		{{! Posts }}

		{{#get "posts" limit="4" filter="tag:growth" include="tags"}}
		{{#foreach posts}}
		{{>item section_item=true show_tags=true}}
		{{/foreach}}
		{{/get}}
	</div>
</div>

<div class="special-section">
	<div class="global-title-wrap">
		
		{{! Title }}
		<h3 class="global-title-medium">{{{t "Conscious {start-italic}leadership.{end-italic}" start-italic="<span>" end-italic="</span>"}}}</h3>

		{{! Link }}
		<a href="{{@site.url}}/leadership/" aria-label="Link to the collection">{{>icons/site/arrow_right}}</a>

	</div>
	<div class="special-wrap">

		{{! Posts }}

		{{#get "posts" limit="4" filter="tag:business" include="tags"}}
		{{#foreach posts}}
		{{>item section_item=true show_tags=true}}
		{{/foreach}}
		{{/get}}
	</div>
</div>

<div class="special-section">
	<div class="global-title-wrap">
		
		{{! Title }}
		<h3 class="global-title-medium">{{{t "Building {start-italic}community.{end-italic}" start-italic="<span>" end-italic="</span>"}}}</h3>

		{{! Link }}
		<a href="{{@site.url}}/community/" aria-label="Link to the collection">{{>icons/site/arrow_right}}</a>

	</div>
	<div class="special-wrap">

		{{! Posts }}

		{{#get "posts" limit="4" filter="tag:community" include="tags"}}
		{{#foreach posts}}
		{{>item section_item=true show_tags=true}}
		{{/foreach}}
		{{/get}}
	</div>
</div>
